---
// Triangle solver + trig now live as sections of Geometry & Setout.
const dest = "/site/geometry#triangles";
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="refresh" content={`0; url=${dest}`} />
    <link rel="canonical" href="/site/geometry" />
    <meta name="robots" content="noindex" />
    <title>Moved — Triangle Solver &amp; Trig</title>
  </head>
  <body>
    <p>The triangle solver has moved to <a href={dest}>Geometry &amp; Setout → Triangles</a>.</p>
    <script is:inline set:html={`location.replace(${JSON.stringify(dest)});`}></script>
  </body>
</html>
