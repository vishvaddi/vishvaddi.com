---
import Base from "../layouts/Base.astro";
---

<Base title="Radio" description="Live radio player — Australian stations.">
  <h1>Radio</h1>
  <p class="radio-kicker">LIVE RECEIVER / AU + WORLD</p>

  <!-- Radio cabinet -->
  <div class="radio-cabinet">
    <div class="radio-brand" aria-hidden="true">
      <span>VISHVADDI</span>
      <span>RX-26</span>
    </div>

    <!-- Top row: speaker grille + LCD -->
    <div class="radio-top">

      <div class="speaker-grille" aria-hidden="true"></div>

      <div class="lcd-display">
        <div class="lcd-station" id="lcd-station">STANDBY</div>
        <div class="lcd-bottom-row">
          <span class="lcd-status" id="lcd-status">STANDBY</span>
          <span class="lcd-signal" id="lcd-signal">▮▮▯▯▯</span>
          <div class="lcd-bars" id="lcd-bars">
            <span class="lbar"></span>
            <span class="lbar"></span>
            <span class="lbar"></span>
            <span class="lbar"></span>
            <span class="lbar"></span>
            <span class="lbar"></span>
            <span class="lbar"></span>
            <span class="lbar"></span>
          </div>
        </div>
      </div>

    </div>

    <!-- Tuner frequency scale -->
    <div class="tuner-scale">
      <div class="tuner-freq-labels" aria-hidden="true">
        <span>88</span>
        <span>94</span>
        <span>100</span>
        <span>106</span>
        <span class="tuner-fm-label">FM</span>
      </div>
      <div class="tuner-track">
        <div class="tuner-ticks" aria-hidden="true">
          <span class="tick"></span><span class="tick"></span><span class="tick tall"></span>
          <span class="tick"></span><span class="tick"></span><span class="tick tall"></span>
          <span class="tick"></span><span class="tick"></span><span class="tick tall"></span>
          <span class="tick"></span><span class="tick"></span><span class="tick tall"></span>
          <span class="tick"></span>
        </div>
        <div class="tuner-needle" id="tuner-needle"></div>
      </div>
    </div>

    <!-- Preset buttons -->
    <div class="presets-row">
      <div id="favourites-list" class="presets-btns"></div>
    </div>

    <!-- Controls row -->
    <div class="controls-row">
      <span class="vol-label">VOL</span>
      <input
        id="volume-slider"
        type="range"
        min="0"
        max="1"
        step="0.02"
        value="0.8"
        class="vol-slider"
        aria-label="Volume"
      />
      <button id="play-pause-btn" class="play-btn" aria-label="Play/pause">&#9654;</button>
    </div>

    <div class="track-id-row" aria-live="polite">
      <div class="track-id-copy">
        <span class="track-id-label">TRACK ID</span>
        <span class="track-id-title" id="track-id-title">NO TRACK DATA YET</span>
      </div>
      <div class="track-id-actions">
        <button id="track-id-btn" class="track-id-btn" type="button">ID</button>
        <button id="shazam-btn" class="track-id-btn shazam-btn" type="button">SHAZAM</button>
      </div>
    </div>

    <!-- Tune row -->
    <div class="tune-row">
      <span class="vol-label">TUNE</span>
      <button
        class="station-toggle"
        id="station-toggle"
        type="button"
        aria-expanded="false"
        aria-controls="station-bay"
      >
        <span class="station-toggle-led" aria-hidden="true"></span>
        <span class="station-toggle-copy">
          <span class="station-toggle-label">CHANNEL SELECT</span>
          <span class="station-toggle-name" id="sp-trigger">SELECT STATION</span>
        </span>
        <span class="station-count" id="station-count">18 CH</span>
        <span class="station-toggle-chevron" aria-hidden="true"></span>
      </button>
    </div>

    <section class="station-bay" id="station-bay" aria-label="Station directory" hidden>
      <div class="station-bay-head">
        <label class="station-search-wrap">
          <span class="sr-only">Search stations</span>
          <span class="station-search-icon" aria-hidden="true"></span>
          <input
            class="station-search"
            id="station-search"
            type="search"
            placeholder="FIND STATION OR GENRE"
            autocomplete="off"
          />
        </label>
        <button class="station-close" id="station-close" type="button" aria-label="Close station directory">CLOSE</button>
      </div>
      <div class="station-filters" aria-label="Filter stations">
        <button class="station-filter active" type="button" data-filter="all">ALL</button>
        <button class="station-filter" type="button" data-filter="preset">PRESETS</button>
        <button class="station-filter" type="button" data-filter="au">AU</button>
        <button class="station-filter" type="button" data-filter="music">MUSIC</button>
        <button class="station-filter" type="button" data-filter="talk">TALK</button>
        <button class="station-filter" type="button" data-filter="chill">CHILL</button>
      </div>
      <div class="station-list-head" aria-hidden="true">
        <span>CH</span>
        <span>STATION / BAND</span>
        <span>SIGNAL</span>
      </div>
      <div class="station-list" id="sp-list" role="listbox" aria-label="Stations"></div>
      <div class="station-empty" id="station-empty" hidden>NO CHANNELS FOUND</div>
      <div class="station-directory-status" id="station-directory-status">LOADING AU DIRECTORY...</div>
    </section>

  </div>
  <!-- end cabinet -->

  <audio id="radio-audio" style="display:none"></audio>
  <script src="/scripts/radio.js" defer></script>

</Base>

<style is:global>
  .radio-kicker {
    margin: -0.55rem 0 0;
    color: var(--muted, #8e867a);
    font-family: monospace;
    font-size: 0.65rem;
    letter-spacing: 0.14em;
    text-align: center;
  }

  /* ── Cabinet shell ── */
  .radio-cabinet {
    position: relative;
    background:
      linear-gradient(160deg, rgba(255,180,50,0.025), transparent 35%),
      linear-gradient(160deg, #1e1408, #130d04);
    border-radius: 16px;
    max-width: 540px;
    margin: 1.5rem auto;
    box-shadow:
      0 0 0 1px #3a2008,
      0 8px 40px rgba(0,0,0,0.7),
      inset 0 1px 0 rgba(255,180,50,0.07);
    padding: 18px 20px 16px;
  }

  .radio-brand {
    display: flex;
    justify-content: space-between;
    margin: -2px 1px 9px;
    color: rgba(200,137,10,0.52);
    font: 600 0.55rem/1 monospace;
    letter-spacing: 0.18em;
  }

  /* ── Top row ── */
  .radio-top {
    display: flex;
    gap: 14px;
    align-items: stretch;
    margin-bottom: 10px;
  }

  /* ── Speaker grille ── */
  .speaker-grille {
    flex-shrink: 0;
    width: 120px;
    min-height: 80px;
    border-radius: 6px;
    border: 1px solid #2a1a08;
    background:
      radial-gradient(circle, rgba(120,80,20,0.55) 1.5px, transparent 1.5px),
      #0c0804;
    background-size: 12px 12px;
    background-position: 6px 6px;
    box-shadow: inset 0 2px 6px rgba(0,0,0,0.6);
  }

  /* ── LCD display ── */
  .lcd-display {
    flex: 1;
    background: #020a02;
    border: 1px solid #0d1a0d;
    border-radius: 6px;
    padding: 10px 14px;
    box-shadow: inset 0 2px 10px rgba(0,0,0,0.8);
    display: flex;
    flex-direction: column;
    justify-content: space-between;
    gap: 8px;
    min-height: 80px;
  }

  .lcd-station {
    color: #2dff4a;
    text-shadow: 0 0 10px rgba(45,255,74,0.5);
    font-family: monospace;
    font-size: 0.9rem;
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
    letter-spacing: 0.04em;
  }

  .lcd-bottom-row {
    display: flex;
    align-items: flex-end;
    gap: 10px;
  }

  .track-id-row {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 10px;
    margin: 8px 0 10px;
    padding: 7px 8px;
    border: 1px solid #2c1a07;
    border-radius: 6px;
    background: rgba(3, 10, 3, 0.72);
    box-shadow: inset 0 1px 5px rgba(0,0,0,0.55);
  }

  .track-id-copy {
    min-width: 0;
  }

  .track-id-label {
    display: block;
    color: rgba(45,255,74,0.42);
    font: 700 0.55rem/1 monospace;
    letter-spacing: 0.16em;
    margin-bottom: 3px;
  }

  .track-id-title {
    display: block;
    max-width: 330px;
    overflow: hidden;
    text-overflow: ellipsis;
    white-space: nowrap;
    color: #2dff4a;
    text-shadow: 0 0 8px rgba(45,255,74,0.35);
    font: 700 0.72rem/1.2 monospace;
    letter-spacing: 0.04em;
  }

  .track-id-actions {
    display: flex;
    gap: 5px;
    flex-shrink: 0;
  }

  .track-id-btn {
    border: 1px solid #573004;
    border-radius: 4px;
    background: linear-gradient(180deg, #2c1905, #130b02);
    color: #e2a83a;
    cursor: pointer;
    font: 700 0.62rem/1 monospace;
    letter-spacing: 0.09em;
    padding: 0.45rem 0.5rem;
    box-shadow: inset 0 1px 0 rgba(255,180,50,0.12);
  }

  .track-id-btn:hover {
    border-color: #a85d0b;
    color: #ffd17a;
  }

  .track-id-btn:disabled {
    opacity: 0.5;
    cursor: wait;
  }

  .lcd-status {
    font-family: monospace;
    font-size: 0.65rem;
    color: rgba(45,255,74,0.55);
    letter-spacing: 0.08em;
    flex-shrink: 0;
  }

  .lcd-signal {
    font-family: monospace;
    font-size: 0.6rem;
    color: rgba(45,255,74,0.4);
    letter-spacing: 0.05em;
    flex-shrink: 0;
  }

  /* ── LCD EQ bars ── */
  .lcd-bars {
    display: flex;
    align-items: flex-end;
    gap: 2px;
    height: 18px;
    margin-left: auto;
  }

  .lbar {
    display: block;
    width: 3px;
    border-radius: 1px;
    background: #2dff4a;
    box-shadow: 0 0 3px rgba(45,255,74,0.4);
    height: 3px;
    transition: height 0.1s ease-out;
  }

  /* ── Tuner scale ── */
  .tuner-scale {
    margin-bottom: 10px;
  }

  .tuner-freq-labels {
    display: flex;
    justify-content: space-between;
    padding: 0 4px;
    margin-bottom: 2px;
  }

  .tuner-freq-labels span {
    font-family: monospace;
    font-size: 0.6rem;
    color: rgba(200,137,10,0.4);
    letter-spacing: 0.03em;
  }

  .tuner-fm-label {
    color: rgba(200,137,10,0.55) !important;
  }

  .tuner-track {
    position: relative;
    background: #0a0604;
    border-radius: 4px;
    height: 22px;
    border: 1px solid #2a1a08;
    overflow: hidden;
  }

  .tuner-ticks {
    position: absolute;
    inset: 0;
    display: flex;
    align-items: flex-end;
    justify-content: space-around;
    padding: 0 8px;
  }

  .tick {
    display: block;
    width: 1px;
    height: 5px;
    background: rgba(200,137,10,0.25);
    margin-bottom: 2px;
  }

  .tick.tall {
    height: 9px;
    background: rgba(200,137,10,0.4);
  }

  .tuner-needle {
    position: absolute;
    top: 0;
    bottom: 0;
    width: 2px;
    background: linear-gradient(to bottom, #ffb432, #c8890a);
    box-shadow: 0 0 6px rgba(255,180,50,0.8);
    border-radius: 1px;
    left: 50%;
    transform: translateX(-50%);
    transition: left 0.6s cubic-bezier(0.25, 0.46, 0.45, 0.94);
  }

  /* ── Preset buttons ── */
  .presets-row {
    margin-bottom: 10px;
  }

  .presets-btns {
    display: flex;
    gap: 6px;
  }

  .preset-btn {
    flex: 1;
    min-width: 0;
    padding: 8px 6px;
    border: 1px solid #3d2510;
    border-radius: 8px;
    background: linear-gradient(to bottom, #291808, #1a0f04);
    color: rgba(255,180,50,0.65);
    font: 0.7rem/1.3 monospace;
    text-align: center;
    cursor: pointer;
    transition: color 0.15s, box-shadow 0.15s, border-color 0.15s, transform 0.15s;
  }

  .preset-btn:hover,
  .preset-btn:focus-visible {
    color: #ffb432;
    border-color: #6a4020;
    outline: none;
  }

  .preset-btn:active { transform: translateY(1px); }
  .preset-btn.active {
    color: #ffb432;
    border-color: #6a4020;
    box-shadow: 0 0 10px rgba(255,180,50,0.25);
  }

  /* ── Controls row ── */
  .controls-row {
    display: flex;
    align-items: center;
    gap: 10px;
  }

  .vol-label {
    font-family: monospace;
    font-size: 0.6rem;
    color: rgba(200,137,10,0.5);
    letter-spacing: 0.1em;
    flex-shrink: 0;
  }

  .vol-slider {
    flex: 1;
    accent-color: #c8890a;
    cursor: pointer;
  }

  .play-btn {
    width: 34px;
    height: 34px;
    border-radius: 50%;
    background: none;
    border: 1.5px solid rgba(255,180,50,0.55);
    color: rgba(255,180,50,0.8);
    font-size: 0.85rem;
    cursor: pointer;
    display: flex;
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
    transition: border-color 0.15s, color 0.15s, box-shadow 0.15s;
  }

  .play-btn:hover {
    border-color: #ffb432;
    color: #ffb432;
    box-shadow: 0 0 8px rgba(255,180,50,0.3);
  }

  /* ── Tune row ── */
  .tune-row {
    margin-top: 10px;
    display: grid;
    grid-template-columns: auto minmax(0, 1fr);
    align-items: center;
    gap: 10px;
  }

  .station-toggle {
    display: grid;
    grid-template-columns: auto minmax(0, 1fr) auto auto;
    align-items: center;
    gap: 9px;
    width: 100%;
    min-width: 0;
    padding: 7px 9px;
    border: 1px solid #3d2510;
    border-radius: 5px;
    background:
      linear-gradient(180deg, rgba(255,180,50,0.035), transparent),
      #0c0804;
    box-shadow: inset 0 1px 4px rgba(0,0,0,0.7);
    color: rgba(255,180,50,0.85);
    cursor: pointer;
    text-align: left;
  }

  .station-toggle:hover,
  .station-toggle:focus-visible,
  .station-toggle[aria-expanded="true"] {
    border-color: #6a4020;
    outline: none;
  }

  .station-toggle-led {
    width: 7px;
    height: 7px;
    border-radius: 50%;
    background: #6e4107;
    box-shadow: inset 0 0 2px rgba(0,0,0,0.8);
  }

  .station-toggle.has-station .station-toggle-led {
    background: #ffb432;
    box-shadow: 0 0 7px rgba(255,180,50,0.7);
  }

  .station-toggle-copy {
    display: flex;
    min-width: 0;
    flex-direction: column;
    gap: 1px;
  }

  .station-toggle-label {
    color: rgba(200,137,10,0.44);
    font: 0.5rem/1 monospace;
    letter-spacing: 0.12em;
  }

  .station-toggle-name {
    min-width: 0;
    color: #ffb432;
    font: 0.72rem/1.25 monospace;
    letter-spacing: 0.03em;
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
  }

  .station-count {
    color: rgba(200,137,10,0.58);
    font: 0.58rem/1 monospace;
    letter-spacing: 0.08em;
    white-space: nowrap;
  }

  .station-toggle-chevron {
    width: 7px;
    height: 7px;
    border-right: 1px solid rgba(255,180,50,0.65);
    border-bottom: 1px solid rgba(255,180,50,0.65);
    transform: rotate(45deg) translateY(-2px);
    transition: transform 0.18s;
  }

  .station-toggle[aria-expanded="true"] .station-toggle-chevron {
    transform: rotate(225deg) translate(-2px, -2px);
  }

  .station-bay {
    margin-top: 9px;
    overflow: hidden;
    border: 1px solid #3d2510;
    border-radius: 7px;
    background:
      repeating-linear-gradient(0deg, rgba(255,180,50,0.012) 0, rgba(255,180,50,0.012) 1px, transparent 1px, transparent 3px),
      #090603;
    box-shadow: inset 0 2px 8px rgba(0,0,0,0.72);
  }

  .station-bay[hidden] { display: none; }

  .station-bay-head {
    display: flex;
    gap: 7px;
    padding: 8px;
    border-bottom: 1px solid rgba(61,37,16,0.65);
  }

  .station-search-wrap {
    position: relative;
    flex: 1;
    min-width: 0;
  }

  .station-search-icon {
    position: absolute;
    top: 50%;
    left: 9px;
    width: 8px;
    height: 8px;
    border: 1px solid rgba(200,137,10,0.55);
    border-radius: 50%;
    transform: translateY(-60%);
    pointer-events: none;
  }

  .station-search-icon::after {
    content: "";
    position: absolute;
    right: -4px;
    bottom: -3px;
    width: 5px;
    border-top: 1px solid rgba(200,137,10,0.55);
    transform: rotate(45deg);
    transform-origin: left;
  }

  .station-search {
    width: 100%;
    border: 1px solid #2f1c0c;
    border-radius: 4px;
    background: #050302;
    color: #ffb432;
    padding: 7px 8px 7px 26px;
    font: 0.65rem/1 monospace;
    letter-spacing: 0.05em;
  }

  .station-search::placeholder { color: rgba(200,137,10,0.35); }
  .station-search:focus {
    border-color: #6a4020;
    outline: none;
    box-shadow: 0 0 0 1px rgba(255,180,50,0.08);
  }

  .station-close {
    border: 1px solid #2f1c0c;
    border-radius: 4px;
    background: #130b04;
    color: rgba(200,137,10,0.56);
    padding: 0 9px;
    font: 0.55rem/1 monospace;
    letter-spacing: 0.08em;
    cursor: pointer;
  }

  .station-close:hover,
  .station-close:focus-visible {
    color: #ffb432;
    border-color: #6a4020;
    outline: none;
  }

  .station-filters {
    display: flex;
    gap: 4px;
    padding: 7px 8px;
    overflow-x: auto;
    border-bottom: 1px solid rgba(61,37,16,0.5);
    scrollbar-width: thin;
  }

  .station-filter {
    flex: 0 0 auto;
    border: 1px solid #2f1c0c;
    border-radius: 999px;
    background: transparent;
    color: rgba(200,137,10,0.46);
    padding: 4px 8px 3px;
    font: 0.52rem/1 monospace;
    letter-spacing: 0.08em;
    cursor: pointer;
  }

  .station-filter:hover,
  .station-filter:focus-visible,
  .station-filter.active {
    border-color: #6a4020;
    color: #ffb432;
    outline: none;
  }

  .station-filter.active { background: rgba(255,180,50,0.07); }

  .station-list-head,
  .station-row {
    display: grid;
    grid-template-columns: 28px minmax(0, 1fr) 54px;
    gap: 8px;
    align-items: center;
  }

  .station-list-head {
    padding: 6px 9px 5px;
    color: rgba(200,137,10,0.32);
    font: 0.48rem/1 monospace;
    letter-spacing: 0.1em;
  }

  .station-list-head span:last-child { text-align: right; }

  .station-list {
    max-height: 270px;
    overflow-y: auto;
    overscroll-behavior: contain;
    scrollbar-color: #4c2d12 #090603;
    scrollbar-width: thin;
  }

  .station-row {
    width: 100%;
    min-width: 0;
    padding: 7px 9px;
    border: 0;
    border-top: 1px solid rgba(61,37,16,0.35);
    background: transparent;
    color: rgba(255,180,50,0.72);
    text-align: left;
    cursor: pointer;
  }

  .station-row:hover,
  .station-row:focus-visible {
    background: rgba(255,180,50,0.055);
    color: #ffb432;
    outline: none;
  }

  .station-row.active {
    background: linear-gradient(90deg, rgba(255,180,50,0.11), rgba(255,180,50,0.02));
    color: #ffb432;
  }

  .station-channel {
    color: rgba(200,137,10,0.38);
    font: 0.58rem/1 monospace;
  }

  .station-row.active .station-channel { color: #ffb432; }

  .station-row-copy {
    display: flex;
    min-width: 0;
    flex-direction: column;
    gap: 2px;
  }

  .station-row-name {
    overflow: hidden;
    font: 0.7rem/1.15 monospace;
    text-overflow: ellipsis;
    white-space: nowrap;
  }

  .station-row-meta {
    overflow: hidden;
    color: rgba(200,137,10,0.36);
    font: 0.51rem/1.1 monospace;
    letter-spacing: 0.04em;
    text-overflow: ellipsis;
    text-transform: uppercase;
    white-space: nowrap;
  }

  .station-signal {
    display: flex;
    align-items: flex-end;
    justify-content: flex-end;
    gap: 2px;
    height: 12px;
  }

  .station-signal span {
    width: 2px;
    background: rgba(200,137,10,0.38);
  }

  .station-signal span:nth-child(1) { height: 3px; }
  .station-signal span:nth-child(2) { height: 5px; }
  .station-signal span:nth-child(3) { height: 8px; }
  .station-signal span:nth-child(4) { height: 11px; }
  .station-row:hover .station-signal span,
  .station-row.active .station-signal span { background: #ffb432; }

  .station-empty,
  .station-directory-status {
    padding: 10px;
    border-top: 1px solid rgba(61,37,16,0.35);
    color: rgba(200,137,10,0.38);
    font: 0.55rem/1 monospace;
    letter-spacing: 0.08em;
    text-align: center;
  }

  .station-directory-status.ready { color: rgba(45,255,74,0.42); }
  .station-directory-status.error { color: rgba(255,121,64,0.62); }

  .sr-only {
    position: absolute;
    width: 1px;
    height: 1px;
    padding: 0;
    margin: -1px;
    overflow: hidden;
    clip: rect(0, 0, 0, 0);
    white-space: nowrap;
    border: 0;
  }

  @media (max-width: 560px) {
    .radio-cabinet {
      margin: 1.1rem -0.25rem;
      padding: 15px 14px 13px;
      border-radius: 12px;
    }

    .radio-top { gap: 10px; }
    .speaker-grille { width: 78px; }
    .lcd-display { padding: 9px 10px; }
    .presets-btns { display: grid; grid-template-columns: 1fr 1fr; }
    .station-count { display: none; }
    .station-list { max-height: 330px; }
  }
</style>
